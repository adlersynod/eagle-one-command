# OpenClaw Gateway
VITE_GATEWAY_URL=http://127.0.0.1:18789
VITE_OPENCLAW_AGENT=openclaw

# When deploying behind nginx (production):
# VITE_GATEWAY_URL=/v1
# (nginx injects Authorization header automatically)
